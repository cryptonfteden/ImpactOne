# Regression Database — Permanent Record

Every regression found across this engagement, with real evidence for each entry. Scoped honestly: this document only records regressions for which the discovering session has direct, verifiable evidence (phase X2 onward, all discovered and fixed within this same continuous engagement). Earlier phases (D1–F2, X1) are not included — fabricating entries for phases outside this record's direct knowledge would violate the project's own "never invent evidence" standard. If earlier regression records exist elsewhere (sprint reports, `*_VERDICT.md` files), they should be merged into this table in a future pass rather than guessed at here.

| # | Phase | Symptom | Root Cause | Detection | Fix | Prevention | Future Risk |
|---|---|---|---|---|---|---|---|
| 1 | X2 | A falling stock could be mis-ranked as `LONG_PRESSURE` in Market Positioning | Non-directional factors (relative volume, liquidity) could override genuine negative momentum in `computePressureScore` — magnitude and direction weren't separated | The service's own test suite, before shipping | Only signed momentum sets direction; volume/liquidity only scale magnitude | Test suite already covers this exact case (`marketPositioningService.test.js`) | Low — direction/magnitude separation is now a documented, tested invariant (`SCORING_ARCHITECTURE.md` Family 3) |
| 2 | X2 | All H3 price-alert-triggering tests reported 0 triggers | `targetPrice` was removed during an `alertTypeRegistry` refactor but still referenced later in the function; silently swallowed by an existing try/catch | Re-running the full H3 test suite after the refactor | Restored the reference; the catch block's over-broad swallowing was the deeper issue | None added specifically — this class of "refactor leaves a stale reference, caught by try/catch" bug has no structural prevention beyond running the full suite after every refactor | Medium — any try/catch that swallows without logging can hide a similar bug; no lint rule enforces logging inside every catch block |
| 3 | X3 | A new tooltip test was flaky | `ResizeObserver` could attach before its observed container existed in the DOM (while the loading spinner still showed), permanently stalling `containerWidth` at 0 in some timing conditions | A flaky new test, root-caused rather than retried into passing | Added `[isLoading, error, bars.length]` as effect dependencies so the observer re-attaches once the container mounts | None structural — this is a real React/DOM timing class of bug; no automated check catches it generically | Low-Medium — any future `useEffect` pairing a `ResizeObserver` with a conditionally-rendered container is at risk of the same class of bug |
| 4 | X3 | Two new frontend tests had ambiguous `getByText` matches (a filter button's label matched a status pill's identical text) | Test used `getByText` where two elements legitimately shared the same visible text | Test run failure (ambiguous match error) | Switched to `getByRole("button", { name })` / `getAllByText` | None structural — a matter of test-writing discipline | Low — visible immediately as a test failure, never silently wrong |
| 5 | X4 | 14 of 18 new HTTP integration tests failed with 400/`undefined` errors | Tests used synthetic `betaUserId` strings (correct for *unit* tests calling services directly) inside *HTTP* integration tests, which go through `betaUserContext` middleware — a synthetic string never resolves to a real `BetaUser` row, so `req.betaUserId` stayed `undefined` | Running the new test files and reading the failures | Added a `test.before()` find-or-create of a real, persisted `BetaUser` row per test file | This distinction (service-level unit tests may use synthetic ids; HTTP tests must use a real persisted row) is now a documented, repeated pattern across every subsequent phase's integration tests | Low — the pattern is now well-established and consistently followed (verified again this phase, `rcCheck.js`'s real browser session hit real identity-required 400s exactly as designed) |
| 6 | X6 | The real production build (`npm run build`) failed with `[MISSING_EXPORT]` | `Header.jsx` imported `BETA_USER_LABEL_STORAGE_KEY` from `BetaInviteGate.jsx` — the export moved to `useBetaIdentity.js` during Phase X4's identity-flow rewrite, and this one import site was never updated | Phase X6's new `releaseValidation.js` script, its first-ever run | Fixed the import to point at the correct module | `releaseValidation.js` now runs before every merge, specifically checking the real production build | **This was live-broken in production from Phase X4 until Phase X6** — `npm run dev` and every Vitest run both tolerated the stale import; only a real production build failed. Confirms the real, ongoing value of a build-based check distinct from unit/component tests |
| 7 | X7 | The real production build failed with `[MISSING_EXPORT] "symbolIntelligenceApi"` | Phase X7's `StockSidePanel.jsx` migration imported `symbolIntelligenceApi` from `services/api/index.js`, but the actual `symbolIntelligenceApi.js` file was never created — Phase X5 built the backend service and a migration *plan* but never the frontend wrapper itself | The same `releaseValidation.js` script, run again this phase | Created the missing `symbolIntelligenceApi.js` file and its export | Same as #6 — this is the release-validation build check doing exactly its job, twice now | Low for this specific file (fixed); the general risk (a new frontend file referenced before it's created) remains open-ended, mitigated only by the build check catching it before merge |
| 8 | X7-RC | Real, live browser session (fresh Playwright context against the real dev server) showed a raw backend error string ("A beta user identity is required for watchlist folders.") directly on the Workspaces screen | `WatchlistFoldersScreen.jsx`'s catch block used `loadError?.message \|\| "friendly fallback"` — since a real caught error almost always has a `.message`, the friendly fallback essentially never fired. Phase X5's `PRIVATE_BETA_POLISH.md` pass fixed this exact anti-pattern on 3 screens (Decision Center, Portfolio, AI Analysis) but a live grep this phase found **9 more untouched instances** across `AdvancedChart.jsx`, `ImpactGraph.jsx`, `WorkspaceDetail.jsx`, `AskImpactOnePanel.jsx`, `AiAnalysisScreen.jsx` (a second instance, not the four X5 fixed), `GlobalIntelligenceScreen.jsx`, `MarketPositioningScreen.jsx`, `WatchlistFoldersScreen.jsx`, `WatchlistScreen.jsx` | **Only found by actually driving a real browser against a real backend with no identity set** — every existing automated test mocks its API layer with a controlled, developer-chosen rejection message, so `err.message` in a test is always something readable like `"network down"`, never the accidentally-technical real backend string a live session produces | All 9 real instances fixed this phase (see `X7_RC_REPORT.md`); their corresponding tests updated to assert the friendly message and explicitly assert the raw message is absent | X5's polish pass was scoped by inspection, not by an exhaustive grep — this phase's fix is exhaustive (`grep -rn "Error?.message \|\| \|error?.message \|\|"` across the whole frontend); **the grep pattern itself is the durable prevention** — re-run it before any future release | Low immediately (swept exhaustively); the pattern itself (`x?.message \|\| fallback`) could reappear in new code — no lint rule forbids it yet (see Root Cause Analysis's prevention recommendation) |
| 9 | Documented since ≈H2, **fixed X7-RC** | `portfolioEngineService.test.js`'s `getPerformanceDelta computes a real value change against yesterday's snapshot` failed intermittently | The test mocked a fixed quote via `withMockedQuote(200)` for the *order placement* step only, but `getPerformanceDelta()` itself calls `getPortfolioSummary()`, which makes its own independent, unmocked live Finnhub call to mark the position to market — a real live-quote drift moved the computed total away from the test's expected `100000` | Documented as a known, reproducible flake across every phase from H2 through X7 (six-plus completion reports); this phase, re-diagnosed precisely and traced to the exact unmocked call | **Fixed**: the `getPerformanceDelta()` call moved inside the same `withMockedQuote` scope as the order placement. Verified stable across 3 consecutive real runs (previously ~13-14s per run due to a live network call; now ~100ms, confirming the live call is gone) | The fix itself is the prevention — no more live network dependency in this test | Closed. Watch for the same *class* of bug (a mock scope not covering every call that needs it) in other tests using `withMockedQuote`-style helpers. |

## Legend

- **Detection** column answers "how was this actually found," not "how it should have been found" — several entries (#6, #7, #8) were found by a *new* verification layer this same engagement built, not by pre-existing tooling.
- **Prevention** column is empty or weak for #2 and #3 — recorded honestly rather than claiming a prevention mechanism that doesn't exist.
